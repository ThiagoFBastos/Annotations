<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title">Ação</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
          </div>
          <div class="modal-body">
            <b>Tem certeza que dejesa excluir esta tag?</b><br>
            Essa ação não poderá ser desfeita.
          </div>
          <div class="modal-footer">
            <a class="btn btn-danger" href = "/tags/delete/{{tag._id}}">Deletar</a>
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Fechar</button>
          </div>
        </div>
    </div>
</div>

<div class = "container">
    <h1 class = "text-center">{{tag.name}}</h1>
    <div class = "text-end">
        <a class = "btn btn-warning" href = "/tags/edit/{{tag._id}}">Editar tag</a>
        <button class = "btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">Excluir tag</button>
    </div>
    <p class = "text-center">
        {{tag.description}}
    </p>
    {{#if annotations}}
        <table class = "table">
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Criação</th>
                </tr>
            </thead>
            <tbody>
                {{#each annotations}}
                    <tr>
                        <td><a href = "{{url}}">{{title}}</a></td>
                        <td>{{date}}</td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
        <nav>
            <ul class = "pagination justify-content-center">
                
                {{#if URL_PREVIOUS }}
                    <li class="page-item">
                        <a class="page-link" href="{{URL_PREVIOUS}}">&lsaquo;</a>
                    </li> 
                {{else}}
                    <li class="page-item disabled">
                        <a class="page-link" href="#" tabindex="-1">&lsaquo;</a>
                    </li>
                {{/if}}

                {{#each navigationPages}}
                    {{#if current }}
                        <li class = "page-item active">
                            <a href = "{{url}}" class = "page-link">{{page}}</a>
                        </li>
                    {{else}}
                        <div class = "page-item">
                            <a href = "{{url}}" class = "page-link">{{page}}</a>
                        </div>
                    {{/if}}
                {{/each}}

                {{#if URL_NEXT}}
                     <li class="page-item">
                        <a class="page-link" href="{{URL_NEXT}}">&rsaquo;</a>
                    </li>
                {{else}}
                     <li class="page-item disabled">
                        <a class="page-link" href="#" tabindex="-1">&rsaquo;</a>
                    </li>
                {{/if}}
            </ul>
        </nav>
    {{/if}}
</div>
